const User = require('./User');
const Subject = require('./Subject');

// Aquí también puedes agregar la lógica para asociar los modelos
// User.associate({ Subject });

module.exports = { User, Subject };